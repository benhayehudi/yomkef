<!DOCTYPE html>
<h2 id="page-title">My Listings</h2>
<div id='flash' class='notice'>
  <%= flash[:notice] %>
</div>


<% @listings = Listing.where("user_id LIKE ?", "%#{params[:id]}%") %>
<% @listings.all.each do |listing| %>
<div class="col-md-6 item-listing">
  <img src="<% if listing.img_url == nil || listing.img_url == "" %>
    <%= "/images/default_listing_img.png" %>
    <% else %>
    <%= listing.img_url %>
    <% end %>" class="img-thumbnail" alt="Yom Kef" align="left">
<strong><h4><a href="/listings/show/<%= listing.id %>"><%= listing.name %></a></h4></strong><a href="#"><a href="/sessions/edit-listing/<%= listing.id %>"> + Edit Listing</a><br><br>
<strong>Location:</strong> <%= listing.location %><br>
<strong>URL:</strong> <a href="<%= listing.url %>" target="_blank"><%= listing.url %></a><br>
<strong>Category:</strong> <%= listing.item_category %><br>
<strong>Description:</strong> <%= listing.description %><br>
</div>
<% end %>
